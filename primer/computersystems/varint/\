import struct
def encode(n):
    return n
    """
    - in a loop:
        take loweest order 7 bits
        add the correct msb

    """
with open('1.uint64','rb') as f:
    print(struct.unpack('>Q',f.read()))
    print(encode(100))

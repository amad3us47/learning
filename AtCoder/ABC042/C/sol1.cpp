#include <bits/stdc++.h>
using namespace std;



int main() {
	int n,k;
	cin >> n >> k;
	std::vector<int> v(n);
	for(int i=0;i<=n;i++) 
	{
		cin >> v[i];
	}

	// taking out digits and finding duplicates
	while(n>0)
	{
		digi=n%10;
		if(d.count(digi
	}
}
